熊出没注意:
  template: |
    【{{ keyword }}】
    {{ datetime }}ごろ、{{ location }}で熊の目撃情報がありました。
    {{ url }}
    {{ extra }}

未定義:
  template: |
    【{{ keyword }}】
    {{ datetime }}、{{ location }}で情報がありました。
    {{ url }}
    {{ extra }}
